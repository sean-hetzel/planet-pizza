import { Typography } from "@mui/joy";

const Recipes = () => {
  return (
    <Typography sx={{ mb: 2, color: "primary.plainColor" }}>
      Manage Recipes
    </Typography>
  );
};

export default Recipes;
